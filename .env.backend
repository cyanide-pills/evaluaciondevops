# EC2 Instance 2 - Backend Configuration
# Use this .env file with: docker-compose -f docker-compose.backend.yml --env-file .env.backend up -d
# Credentials should come from GitHub Secrets

DB_HOST=<DATABASE_EC2_IP>
DB_PORT=3306
DB_NAME=despacho_db
DB_USER=despacho_user
SPRING_DDL_AUTO=update
