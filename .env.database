# EC2 Instance 1 - Database Configuration
# Use this .env file with: docker-compose -f docker-compose.db.yml --env-file .env.database up -d
# Passwords should come from GitHub Secrets

MYSQL_DATABASE=despacho_db
MYSQL_USER=despacho_user
